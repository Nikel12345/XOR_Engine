#pragma once
// Компоненты, принадлежащие ИГРЕ, а не движку: объявление, схема и регистрация целиком здесь.
// Движок про них не знает — реестр открыт (ComponentSpecRegistry::Register), и одной записи
// хватает, чтобы компонент поехал в scene.json, появился чекбоксом в форме создания энтити и
// секцией в инспекторе. Ни один файл движка при добавлении такого компонента не правится.
#include "ComponentSerializer.h"

struct MassComponent {
    float mass = 1.0f;
};

// Источник гравитации: центральное тело, к которому притягиваются сущности со скоростью.
// Центр НЕ константа в игровом коде — это ПОЗИЦИЯ самой сущности (трансляция её Positions),
// а сила — поле gm. Отсюда следует всё остальное: центров может быть несколько (ускорения
// складываются), центр видно в редакторе и можно двигать гизмо, а сохранение/загрузка сцены
// его переносят как обычную сущность.
//
// gm = G·M (μ, стандартный гравитационный параметр). ОБЯЗАН совпадать с GM в
// scripts/scene_gen/scene_gen.py: круговые скорости кубам раздаёт генератор по sqrt(GM/r),
// при другом gm орбиты станут эллиптичными/раскрутятся. Форму орбиты задаёт ТОЛЬКО gm.
struct GravityComponent {
    float gm = 5000.0f;
};

// Звать ОДИН раз на старте игры и обязательно ДО первой загрузки сцены: LoadScene резолвит
// компоненты по имени через реестр, незарегистрированный ключ в файле молча пропускается.
// Только на старте: specs_ — вектор, поздняя регистрация переаллоцирует его и протухнут
// указатели на спеки, которые держат форма создания и CreateEntityFromSpecs.
void RegisterGameComponents();
